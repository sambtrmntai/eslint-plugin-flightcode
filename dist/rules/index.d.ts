import type { TSESLint } from "@typescript-eslint/utils";
/**
 * The plugin's own custom rules, keyed by their unprefixed rule id.
 */
export declare const rules: Record<string, TSESLint.RuleModule<string, unknown[]>>;
